# 拾刻 Shike · 把第二故乡，刻进木头里

> 你说一句话、传一张照片，或从图库挑一张剪影，AI 把它变成一个能被激光机真实切出来的矢量稿。
> 情绪交给 AI，几何交给算法。

---

## 一、我们为什么要做这个（动机与痛点）

这个项目的起点，来自一个非常具体的人群：**留学生与异乡打拼的人**。

每个离开家乡、在另一座城市生活过的人，都经历过同一种「带不走」的遗憾——离开时，你装得走行李，却装不走那座城里度过的日子：住过的街区、常去的路口、傍晚的风、某一栋再也回不去的楼。照片会褪色，记忆会模糊，但人们总想为那段生活留下一个「可触摸」的实体。

传统纪念品解决不了这个问题：千篇一律的冰箱贴、明信片，与「你」没有任何关系。而真正「属于你的那份记忆」，门槛又太高——你要会画图、会建模、懂激光切割，才能把它做成一件东西。

于是我们把问题收敛成一句话：

> **如何让一个完全不懂设计、不懂制造的人，只用一句话或一张照片，就把自己记忆里的那座城，变成一件可以真实制造出来的纪念品？**

这就是「拾刻」的初衷——从「给留学生做一件独一无二的纪念品」出发，最终扩展到「每一个有第二故乡的人」。它不是一张 AI 生成的图片，而是一份**真正能落地的、可制造的矢量文件**，承载的是「情绪」，落地的是「几何」。

---

## 二、整体方案设计

拾刻的核心思路只有一句话：**把「情绪表达」交给 AI，把「制造正确性」交给确定性算法，两者之间用一条可审计的链路串联起来。**

整个产品是一条**端到端的智能体工作流**，而不是一个简单的「文字生成图片」工具。它从用户的模糊输入出发，一路自动推进到「能制造为止」，中间每一步都可见、可回放、可降级。

### 产品形态

- 一个**全栈 Web 应用**（Next.js 16 + React 19 + TypeScript），无需安装、打开即用。
- 首页是一张**满屏世界地图**，鼠标滑到哪，地图就在哪亮起一圈柔和蓝光——用「寻找一座城」的交互，唤起用户对那座城的记忆。
- 三条入口，覆盖不同人群：

| 入口 | 适合谁 | 输入 | 产出 |
|------|--------|------|------|
| **① 说一句那座城** | 愿意说、有具体记忆 | 一句话（如「我想念悉尼海港边的歌剧院」） | AI 生成的地标镂空剪影 |
| **② 刻下一张照片** | 有照片、不想描述 | 一张照片 | 主体抠出的镂空雕刻稿 |
| **③ 从图库挑一张** | 没照片、不想打字 | 点选内置剪影 | 秒出的可雕刻镂空稿 |

### 技术架构（分层）

```
用户输入（一句话 / 照片 / 剪影）
        │
        ▼
┌─────────────────────────────────────────┐
│  AI 智能体层（想）                          │
│  DeepSeek ReAct（≤3 轮）                   │
│  · 标准化城市 → 检索真实地标 → 修订简报      │
│  Wikimedia 中英文地标检索                   │
│  Qwen-Image 3.0（画）                      │
│  · 生成地标剪影 / 提取照片主体               │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  确定性算法层（算）                          │
│  Potrace 矢量化 → 闭合复合路径              │
│  Otsu 自适应二值化 + 连通域清理              │
│  参数化天际线兜底                           │
└─────────────────────────────────────────┘
        │
        ▼
┌─────────────────────────────────────────┐
│  制造保障层（验）                            │
│  DFM 6 项确定性检查                        │
│  → SVG 下载 / 分享小红书 / 激光机直接雕刻    │
└─────────────────────────────────────────┘
```

### 关键设计原则

1. **AI 会失败，但用户不必等**：三条链路都设计了多层降级。AI 挂掉或未配置 key 时，自动切到本地确定性算法（参数化天际线、Otsu 二值化），用户永远能拿到一个可制造的结果。
2. **AI 的思考是可见的**：ReAct 的每一步决策都在前端逐条回放，不是黑盒出图。
3. **制造正确性由算法兜底**：AI 负责「像不像、有没有味道」，确定性算法负责「能不能切得出来」，两者职责清晰，互不甩锅。

---

## 三、AI 具体用在哪里（应用与实现细节）

这是本项目最核心的部分。拾刻**不是**简单调用一个大模型画图，而是把多个模型协同起来，组成一个有「思考 → 检索 → 生成 → 校验 → 自修复」闭环的智能体。

### 3.1 双模型协同：一个负责「想」，一个负责「画」

| 模型 | 角色 | 具体职责 |
|------|------|---------|
| **DeepSeek**（deepseek-flash） | 想 | 城市识别、意图理解、ReAct 选景调度 |
| **Qwen-Image 3.0**（阿里云百炼） | 画 | 生成地标镂空剪影、提取照片主体 |

### 3.2 DeepSeek ReAct 智能体（入口一的核心）

用户说一句话，不是一个「文生图」调用，而是一个**最多 3 轮的 ReAct（Reasoning + Acting）循环**：

1. **第一轮 · 标准化地点**：DeepSeek 把用户模糊的自然语言（城市、情感）标准化成结构化的「场景简报」——城市、国家、明确主体、可视特征（visualAnchors）、构图指令。
2. **真实地标检索（Act）**：Agent 不会凭空编造建筑，而是**先联网检索**。代码里调用 **Wikimedia 中英文双路检索**，搜索这座城市的真实地标候选（如「悉尼歌剧院」）。
3. **修订简报（Observe + Revise）**：拿到检索结果后，Agent 观察候选地标，修订简报——选定一个真实主体、提炼 2~4 个可视特征、给出构图指令。
4. **定稿（Finish）**：简报具备「真实单主体 + 足够可视特征 + 无虚构」后，才输出给图像模型。

**几个工程上的关键细节：**

- **只输出结构化 JSON，不暴露私有思维链**：`system` 提示词强制 Agent 每轮只输出 `{action, decision, brief}`，`decision` 是「不超过 120 字的可见决策摘要」。这样既保留了可解释性，又避免了 CoT 私有推理泄露的问题。
- **动作被约束在三个枚举值内**：`search_landmarks | revise_brief | finish`，用 `zod` 式的严格字段校验（`updateSceneBrief` 逐字段类型检查），防止 Agent 输出越界。
- **防虚构约束**：提示词明确要求「禁止拼接不相邻地标、禁止虚构建筑、禁止添加背景碎片」，并配合「用户明确主体 > 检索证据 > 高置信典型场景」的优先级，把幻觉压到最低。
- **全程可审计**：每一轮的 `action / decision / observation` 都以事件流的形式推给前端，用户能实时看到「检索到了什么 → 为什么这么定 → 看到了什么」。

### 3.3 Qwen-Image 生成「激光切割友好」的剪影

图像生成不是随便画一张图，而是用**强约束的 prompt** 让模型输出可直接矢量化的黑白镂空稿：

- **入口一（场景生成）**：prompt 里硬性要求「白底、纯黑主体、高对比、无文字、无渐变」，并针对「激光切割」做专门约束——「白色区域是要被切掉的孔洞，必须被黑色主体完全包围」「每个黑色区域和白色孔洞都要有完整闭合边界，不允许开放的笔画或断开的线头」。同时区分「城市建筑」和「自然景观」两种主体类型，用不同的构图策略。
- **入口二（照片主体提取）**：用 Qwen-Image 的**图像编辑能力**抠出单一主体，prompt 要求「清除整个背景和所有无关物体、人物、阴影、纹理」，并把主体保留为「纯黑连通块」，且**必须包含 2~8 个来自真实结构负空间的大号白色镂空**（这是激光雕刻能成立的关键）。

### 3.4 AI 自修复闭环（AI 不只是一次性输出）

两条链路都有**「生成 → 校验 → 反馈 → 修复」的闭环**：

- **照片入口**：Qwen 抠图后，本地矢量审计会检查「有没有闭合轮廓、孔洞数量、主体占比」；不达标时，把审计结果作为 feedback 回传给 Qwen，**最多自动修复 3 轮**（`repair_holes`），直到出现足够大、足够清晰的闭合镂空。
- **场景入口**：ReAct 的 `finish` 动作前会做 `briefAudit` 检查（「缺少明确主体」「可视特征少于 2 个」就不让 finish），强制 Agent 补全简报。

### 3.5 AI 之后的确定性算法（让 AI 的成果「可制造」）

AI 只负责「像不像」，真正的「制造正确性」由经典算法保证：

- **Potrace 矢量化**：把 AI 生成的像素图，追踪成工业级的**闭合复合矢量路径**（`fill-rule=evenodd`），外轮廓是黑色主体、内部孔洞是白色镂空——这正好是激光切割机的原生语义。
- **Otsu 自适应二值化 + 连通域清理**：照片兜底时，自动算阈值、删除与主体断开的黑色孤岛，输出干净的黑白主体。
- **DFM（面向制造的设计）检查**：生成结果会过 **6 项确定性制造检查**——设备安全包络、切割线宽、矢量线宽、孔径、孔间距等，全部基于真实激光设备的物理参数（幅面、最小孔径、最小间距）。这确保了「AI 画的东西，真的切得出来」。

### 3.6 一句话总结 AI 的定位

> **DeepSeek 负责「想」、Qwen-Image 负责「画」、Wikimedia 检索负责「真」、Potrace/DFM 负责「能制造」。** AI 解决的是「情感表达」和「内容创造」的难题，确定性算法解决的是「工程落地」的难题，两者缺一不可。

---

## 四、团队成员与分工

本项目为**个人独立完成**，所有设计、开发、部署工作由以下成员承担：

| 成员 | 负责内容 |
|------|---------|
| **（作者本人）** | 全部工作：产品构思与设计、全栈开发（前端 UI / 后端 API / 智能体逻辑）、AI 模型接入与 Prompt 工程、矢量与制造算法、部署与运维、文档编写 |

具体覆盖：

- **产品设计**：从「留学生纪念品」的洞察出发，定义三条入口、Apple 流体交互、中英双语。
- **前端**：Next.js + React 19 + framer-motion，满屏地图交互、毛玻璃材质、弹簧动画、逐笔描画揭示。
- **后端与智能体**：DeepSeek ReAct 调度、Wikimedia 检索、Qwen-Image 接入、多层降级、IP 限流、隐私脱敏。
- **算法**：Potrace 矢量化、Otsu 二值化、参数化天际线、DFM 制造检查。
- **部署**：EdgeOne Pages 全栈部署、环境变量管理、生产 Bug 排查修复。

---

## 五、项目的几个亮点（总结）

1. **不是「文生图」，是「可制造的矢量」**：产出物不是一张图片，而是一份激光机直接能切的 SVG，AI 的情绪表达最终落地为真实的制造文件。
2. **AI 思考可见、可审计**：ReAct 每一步决策逐条回放，且刻意只暴露「可见决策摘要」、不泄露私有思维链——兼顾可解释性与安全。
3. **双模型 + 真实检索**：DeepSeek 想、Qwen 画、Wikimedia 保证地标真实，从源头压制 AI 幻觉。
4. **AI 会失败，但体验不崩**：多层降级让产品在无 key、无网络、AI 报错时依然完整可用。
5. **制造正确性由确定性算法兜底**：DFM 6 项检查基于真实设备物理参数，确保「AI 画的，真的切得出来」。

---

> 把忘不掉的那座城，刻进木头里。
